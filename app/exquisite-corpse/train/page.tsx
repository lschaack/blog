import AdminView from '@/app/components/AdminView';

type GameViewerProps = {
  page: number;
  pageSize: number;
}
function GameViewer({ page, pageSize }: GameViewerProps) {

}

export default async function TrainingInterface() {
  return (
    <AdminView>
      eyyy
    </AdminView>
  );
}
